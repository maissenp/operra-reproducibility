/*
 * Minimal Nextflow config for the module 3 demo.
 */

nextflow.enable.dsl = 2

params {
  outdir = 'results'
}

process {
  // Keep it simple and predictable for a workshop.
  shell = ['/bin/bash', '-euo', 'pipefail']
}
